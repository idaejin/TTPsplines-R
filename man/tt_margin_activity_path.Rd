% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margin_activity_path.R
\name{tt_margin_activity_path}
\alias{tt_margin_activity_path}
\title{Margin Activity Path: score and optionally select TT margins}
\usage{
tt_margin_activity_path(
  y,
  X,
  lambda_path = 10^seq(2, -1, length.out = 7),
  rank = 2L,
  k = 5L,
  degree = 3L,
  penalty_order = 2L,
  family = stats::gaussian(),
  optimizer = "auto",
  backend = "auto",
  control = tt_control(),
  offset = NULL,
  weights = NULL,
  reference = c("median", "mean"),
  n_grid = 40L,
  grid = NULL,
  select = c("1se", "min", "none"),
  folds = 5L,
  foldid = NULL,
  select_lambda = "cGCV",
  refit = TRUE,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{y, X}{Response and \(n\times d\) covariate matrix (as in \code{\link[=ttps]{ttps()}}).}

\item{lambda_path}{Numeric vector of isotropic \(\lambda\) values for the
screening path (default \code{10^seq(2, -1, length.out = 7)}).}

\item{rank, k, degree, penalty_order, family, optimizer, backend, control, offset, weights}{Passed to \code{\link[=ttps]{ttps()}} on screening fits (and on nested CV / refit when
applicable). For subsets with \(d=1\), a univariate \code{smooth.spline} GCV
fallback is used because \code{\link[=ttps]{ttps()}} requires \(d\ge 2\).}

\item{reference}{How to fix \(x_{-j}\) when forming partials: \code{"median"}
(default) or \code{"mean"}.}

\item{n_grid}{Grid size for partial-range evaluation (default \code{40}).}

\item{grid}{Optional numeric vector used as the evaluation grid for each
partial when supplied; otherwise each margin uses an interior quantile
grid of length \code{n_grid} (or a fixed interior grid on approximately unit
interval covariates).}

\item{select}{Selection rule after ranking: \code{"1se"} (default), \code{"min"}, or
\code{"none"} (return scores / ranking only).}

\item{folds}{Number of CV folds (default \code{5}). Ignored if \code{foldid} is set.}

\item{foldid}{Optional fold ids (length \(n\)), as in \code{\link[=tt_rank_select]{tt_rank_select()}}.}

\item{select_lambda}{Smoothing for nested CV and final refit (default
\code{"cGCV"}).}

\item{refit}{If \code{TRUE} (default) and \code{select != "none"}, refit \code{\link[=ttps]{ttps()}} on
the selected margins with \code{select_lambda}.}

\item{seed}{Optional RNG seed for fold assignment when \code{foldid} is \code{NULL}.}

\item{verbose}{Print progress messages.}
}
\value{
An object of class \code{"tt_margin_activity_path"} with components
\code{scores}, \code{activity}, \code{lambda_path}, \code{order}, \code{selected}, \code{m_hat},
\code{cv} (data frame or \code{NULL}), \code{fit} (or \code{NULL}), \code{call}, and metadata.
}
\description{
Screens continuous covariates for a Tensor-Train P-spline by measuring
\strong{partial-range activity} along an isotropic \(\lambda\) path, ranking
margins, and (optionally) choosing the nested top-\(m\) subset by \(K\)-fold
CV with a one-standard-error or min-CV rule. Final smoothing still uses
\code{\link[=ttps]{ttps()}} with \code{lambda = "cGCV"} (or a user choice) on the selected columns.
}
\details{
The method name is \strong{Margin Activity Path} (avoid the acronym "MAP", which
collides with \emph{maximum a posteriori}). Protocol:
\code{inst/benchmarks/margin_path/PROTOCOL_MAP.md}.
}
\examples{
set.seed(1)
n <- 120
d <- 6
X <- matrix(runif(n * d), n, d)
colnames(X) <- paste0("x", seq_len(d))
y <- sin(2 * pi * X[, 1]) + sin(2 * pi * X[, 2]) + rnorm(n, sd = 0.3)
path <- tt_margin_activity_path(
  y, X, rank = 2, k = 5,
  lambda_path = 10^c(1, 0, -1),
  select = "1se", folds = 3, seed = 1,
  control = tt_control(max_sweeps = 4, compute_edf = FALSE, outer_maxit = 4),
  verbose = FALSE
)
path
path$selected

}
\seealso{
\code{\link[=ttps]{ttps()}}, \code{\link[=tt_rank_select]{tt_rank_select()}}, \code{\link[=plot.tt_margin_activity_path]{plot.tt_margin_activity_path()}}
}
