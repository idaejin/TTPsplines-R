% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{tt_truncate_rank}
\alias{tt_truncate_rank}
\title{Truncate TT cores to a lower (or equal) rank by sequential SVD.}
\usage{
tt_truncate_rank(cores, rank)
}
\arguments{
\item{cores}{List of TT core arrays (length \code{d}), as in a \code{"ttpspline"} fit
(\code{fit$cores}), or from \code{\link[=tt_initialize]{tt_initialize()}}.}

\item{rank}{Target rank (scalar, length \code{d-1}, or full chain); see \code{\link[=tt_rank]{tt_rank()}}.}
}
\value{
List of truncated cores with attribute \code{ranks}.
}
\description{
Diagnostic / warm-start helper: left-orthogonalize, then truncate bond
dimensions right-to-left to the target \code{\link[=tt_rank]{tt_rank()}} chain. The contracted
coefficient map is approximated (not exact unless singular values beyond
the cut are zero). Use to warm-start a lower-rank \code{\link[=ttps]{ttps()}} fit from a
higher-rank solution, e.g. Ishigami \(r=3\to r=2\).
}
\examples{
init3 <- tt_initialize(d = 3, rank = 3, k = 6, seed = 1)
init2 <- tt_truncate_rank(init3, rank = 2)
attr(init2, "ranks")
}
