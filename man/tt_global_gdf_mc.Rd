% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/global_gcv_lab.R
\name{tt_global_gdf_mc}
\alias{tt_global_gdf_mc}
\title{Monte Carlo estimate of global GDF of a fixed-rank TT fitter.}
\usage{
tt_global_gdf_mc(
  fit_base,
  y = NULL,
  refit_fun = NULL,
  M = 5L,
  epsilon_rel = 0.001,
  scheme = c("forward", "central"),
  probe_seed = 1L,
  probes = NULL,
  warm_start = TRUE,
  gdf_init = NULL,
  on_nonconverged = c("error", "warn", "na"),
  control = NULL,
  fit_backend = NULL,
  ...
)
}
\arguments{
\item{fit_base}{A Gaussian \code{"ttpspline"} fit with \strong{fixed} numeric \code{lambda}.}

\item{y}{Response used for the base fit (defaults to \code{fit_base$y}).}

\item{refit_fun}{Function \verb{(y_new, fit_base, ...)} returning a \code{"ttpspline"}
(default: warm-started \code{\link[=ttps]{ttps()}} ALS at the same rank / lambda).}

\item{M}{Number of Monte Carlo probes.}

\item{epsilon_rel}{Relative FD step (see \code{.tt_lab_epsilon()}).}

\item{scheme}{\code{"forward"} or \code{"central"}.}

\item{probe_seed}{Seed for Rademacher draws (RNG state restored).}

\item{probes}{Optional \verb{n x M} probe matrix (overrides \code{probe_seed} / \code{M}).}

\item{warm_start}{Legacy alias (\code{TRUE}→\code{probe_warm}, \code{FALSE}→\code{cold}).}

\item{gdf_init}{\code{"probe_warm"} (clone \code{fit_base$cores} per probe) or \code{"cold"}.
Probes never chain; \code{fit_base$cores} must remain unchanged.}

\item{on_nonconverged}{\code{"error"}, \code{"warn"}, or \code{"na"} (record NA contribution).}

\item{control}{Optional \code{\link[=tt_control]{tt_control()}} override for perturbed refits.}

\item{fit_backend}{Lab ALS backend for default refits (\code{"R"} / \code{"Rcpp_fixed"}).}

\item{...}{Passed to \code{refit_fun}.}
}
\value{
Diagnostic list with \code{gdf}, contributions, SE, probes, etc.
}
\description{
Experimental / internal. Perturbs the full converged map
\eqn{\hat y_{\boldsymbol\lambda}=\mathcal A_r(y;\boldsymbol\lambda)}
with shared Rademacher probes. Does \strong{not} use conditional core EDFs.
}
\keyword{internal}
