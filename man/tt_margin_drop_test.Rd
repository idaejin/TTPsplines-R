% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margin_drop_test.R
\name{tt_margin_drop_test}
\alias{tt_margin_drop_test}
\title{Leave-one-margin-out tests for dropping TT covariates}
\usage{
tt_margin_drop_test(
  y,
  X,
  margins = NULL,
  rank = 2L,
  k = 5L,
  degree = 3L,
  penalty_order = 2L,
  lambda = 1,
  family = stats::gaussian(),
  optimizer = "auto",
  backend = "auto",
  control = tt_control(compute_edf = TRUE),
  offset = NULL,
  weights = NULL,
  method = c("nested", "permute"),
  B = 99L,
  alpha = 0.05,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{y, X}{Response and covariate matrix (as in \code{\link[=ttps]{ttps()}}).}

\item{margins}{Integer indices or character names to test (default: all).}

\item{rank, k, degree, penalty_order, lambda, family, optimizer, backend, control, offset, weights}{Passed to \code{\link[=ttps]{ttps()}} (univariate \code{smooth.spline} when a reduced model has
one column).}

\item{method}{\code{"nested"} or \code{"permute"}.}

\item{B}{Permutations when \code{method = "permute"} (default \code{99}).}

\item{alpha}{Soft drop threshold (default \code{0.05}).}

\item{seed}{RNG seed for permutations.}

\item{verbose}{Progress messages.}
}
\value{
Class \code{"tt_margin_drop_test"} with \code{results}, \code{keep},
\code{drop_candidates}, and \code{fit_full}.
}
\description{
For each candidate margin \(j\), compare the full TT fit on all columns of
\code{X} to the reduced fit that \strong{omits} column \(j\). Large improvement from
keeping \(j\) argues against dropping it.
}
\details{
\strong{Null hypothesis (per margin):} margin \(j\) does not improve fit given
the others (candidate to eliminate). Soft rule: \code{p_value > alpha} marks
\code{drop_candidate = TRUE}.

Two approximate procedures:
\itemize{
\item \code{"nested"} (default): \(\Delta_j = D_{-j}-D_{\mathrm{full}}\) with an F-like
reference using joint linearized EDF when available. \emph{Approximate} for TT.
\item \code{"permute"}: permute column \(j\) under the full design \code{B} times; compare
the leave-one-out \(\Delta_j\) to the null importance
\(D_{\mathrm{full}}(x_j^{\pi})-D_{\mathrm{full}}\).
}

Prefer \code{\link[=tt_margin_activity_path]{tt_margin_activity_path()}} for formal subset choice; use this as a
complementary drop diagnostic.
}
\examples{
set.seed(1)
n <- 100
X <- matrix(runif(n * 4), n, 4)
colnames(X) <- paste0("x", 1:4)
y <- sin(2 * pi * X[, 1]) + 0.5 * sin(2 * pi * X[, 2]) + rnorm(n, sd = 0.3)
tst <- tt_margin_drop_test(
  y, X, rank = 2, k = 5, lambda = 1, method = "nested",
  control = tt_control(max_sweeps = 4, compute_edf = TRUE, backend = "R"),
  verbose = FALSE
)
tst

}
\seealso{
\code{\link[=tt_margin_activity_path]{tt_margin_activity_path()}}, \code{\link[=tt_edf]{tt_edf()}}, \code{\link[=ttps]{ttps()}}
}
