% Generated by roxygen2: do not edit by hand
\name{tt_cgcv_frozen_curves}
\alias{tt_cgcv_frozen_curves}
\title{Frozen-fit conditional cGCV curves}
\usage{
tt_cgcv_frozen_curves(
  fit,
  y = NULL,
  X = NULL,
  basis = NULL,
  grid = NULL,
  weight = NULL,
  z = NULL,
  offset = NULL,
  penalty_order = NULL
)
}
\arguments{
\item{fit}{A \code{"ttpspline"} fit (preferably fixed-\eqn{\lambda}) or an
internal ALS/PIRLS list with \code{cores}, \code{lambda}, \code{intercept}.}

\item{y, X, basis}{Optional; taken from \code{fit} when available.}

\item{grid}{Numeric \eqn{\lambda} grid (default log-spaced over search bounds).}

\item{weight}{Optional observation / PIRLS weights.}

\item{z}{Optional working response (GLM); default uses \code{y}.}

\item{offset}{Optional offset.}

\item{penalty_order}{Difference order (default from \code{fit}).}
}
\value{
List with \code{proposals}, \code{curves}, \code{lambda_frozen},
  \code{penalty_mode}.
}
\description{
Fit (or reuse) a TT model at fixed \eqn{\boldsymbol\lambda}, freeze cores,
then evaluate each margin's conditional GCV on a log-grid using the same
frozen \eqn{P_{k,-k}} / design. Does \strong{not} update any \eqn{\lambda_k}.
}
\seealso{
\code{\link{ttps}}, \code{\link{tt_control}}
}
