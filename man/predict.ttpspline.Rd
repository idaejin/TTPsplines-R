% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{predict.ttpspline}
\alias{predict.ttpspline}
\title{Predict from a TT P-spline fit.}
\usage{
\method{predict}{ttpspline}(
  object,
  newdata = NULL,
  type = c("link", "response"),
  offset = NULL,
  linear = NULL,
  smooth = NULL,
  se.fit = FALSE,
  interval = c("none", "confidence"),
  level = 0.95,
  vcov_type = c("bayesian", "frequentist"),
  full_cov = FALSE,
  ...
)
}
\arguments{
\item{object}{A \code{"ttpspline"} fit.}

\item{newdata}{Optional covariate matrix (same columns / order as training).
Default uses the training design.}

\item{type}{\code{"link"} or \code{"response"}.}

\item{offset}{Optional offset for \code{newdata} (default: training offset, or 0).}

\item{linear, smooth}{Optional overrides for parametric / additive smooth
terms when present on \code{object}.}

\item{se.fit}{If \code{TRUE}, return list with \code{fit} and \code{se.fit}.
\code{se.fit} is always on the \strong{link} scale.}

\item{interval}{\code{"none"} or \code{"confidence"} (pointwise, conditional on
rank and lambda; not simultaneous bands). For GLMs, intervals are
formed on the link scale then mapped with the inverse link when
\code{type = "response"}.}

\item{level}{Confidence level for intervals.}

\item{vcov_type}{\code{"bayesian"} (default for \code{se.fit}) or \code{"frequentist"}.}

\item{full_cov}{Reserved; must be \code{FALSE} (no m-by-m prediction covariance).}

\item{...}{Unused.}
}
\value{
A numeric vector of predictions, or a list when \code{se.fit} / intervals
are requested.
}
\description{
Point predictions on the link or response scale. With \code{se.fit = TRUE},
returns conditional (rank-and-lambda fixed) pointwise standard errors on
the \strong{link} scale; see vignette \code{uncertainty}.
}
\seealso{
\code{\link[=vcov.ttpspline]{vcov.ttpspline()}}, \code{\link[=ttps]{ttps()}}, vignette("uncertainty")
}
