% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_functions.R
\name{simulate_dette}
\alias{simulate_dette}
\title{Simulate a Dette--Pepelyshev 8D point-process / Poisson sample.}
\usage{
simulate_dette(
  n_events = 500,
  d = 4L,
  n_bins = 12L,
  rate_scale = 3,
  rate_offset = 0,
  holdout_frac = 0.1,
  seed = 0L
)
}
\arguments{
\item{n_events}{Approximate number of Poisson events (controls \code{rate_scale}).}

\item{d}{Number of active dimensions (1--8; remaining fixed at 0.5).}

\item{n_bins}{Number of grid bins per axis.}

\item{rate_scale}{Scalar that controls the overall event rate.}

\item{rate_offset}{Log-intensity offset (shifts overall level).}

\item{holdout_frac}{Fraction of occupied cells held out for testing.}

\item{seed}{RNG seed.}
}
\value{
A list with components:
\describe{
\item{\code{X_train}, \code{X_test}}{Cell centres (\eqn{n \times d}) for
training and test occupied cells.}
\item{\code{y_train}, \code{y_test}}{Event counts.}
\item{\code{eta_true}}{True log-intensity function (closure).}
\item{\code{d}, \code{n_bins}, \code{n_events}, \code{seed}}{Metadata.}
}
}
\description{
Inputs are drawn uniformly on \eqn{[0,1]^8}.  For \code{d < 8}, only the first
\code{d} inputs vary; the rest are fixed at 0.5 (matching the PDF experiment).
}
