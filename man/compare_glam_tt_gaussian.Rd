% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glam.R
\name{compare_glam_tt_gaussian}
\alias{compare_glam_tt_gaussian}
\title{Compare Currie–Durbán–Eilers Gaussian GLAM vs TT-P-splines on a grid.}
\usage{
compare_glam_tt_gaussian(
  d = 3L,
  n_grid = NULL,
  k = NULL,
  ranks = 1:3,
  lambda = 1,
  sigma = 0.25,
  max_sweeps = 10L,
  seed = 51L,
  backend = "R"
)
}
\arguments{
\item{d}{Dimension (number of margins), typically 3 or 5.}

\item{n_grid}{Grid size per margin (scalar or length-\code{d}). Defaults are
modest for vignettes (\code{d=3} → 14×12×10; \code{d=5} → \(6^5\)).}

\item{k}{Basis size per margin.}

\item{ranks}{Integer vector of TT ranks to try.}

\item{lambda}{Fixed smoothing (scalar or length-\code{d}).}

\item{sigma}{Gaussian noise sd.}

\item{max_sweeps}{ALS sweeps for TT fits.}

\item{seed}{RNG seed.}

\item{backend}{TT backend (\code{"R"} / \code{"Rcpp"} / \code{"auto"}).}
}
\value{
A \code{data.frame} of comparison rows (one GLAM + one per rank).
}
\description{
Builds a regular \code{d}-way design, a non-additive truth, Gaussian noise, then
fits fixed-\(\lambda\) GLAM (full coefficient array) and \code{ttpspline()} at
several TT ranks. Reports parameter counts, compression, RMSE (truth / data /
vs GLAM), and wall time.
}
\examples{
tab3 <- compare_glam_tt_gaussian(d = 3, ranks = 1:3, max_sweeps = 6)
tab3[, c("method", "npar", "compression", "rmse_truth", "time_s")]
}
