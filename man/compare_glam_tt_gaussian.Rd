% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glam_compare.R
\name{compare_glam_tt_gaussian}
\alias{compare_glam_tt_gaussian}
\title{Compare Currie–Durbán–Eilers Gaussian GLAM vs TT-P-splines.}
\usage{
compare_glam_tt_gaussian(
  d = 3L,
  n_grid = NULL,
  k = NULL,
  ranks = 1:3,
  lambda = 1,
  sigma = 0.25,
  max_sweeps = 10L,
  max_glam_npar = 5000L,
  degree = 3L,
  seed = 51L,
  backend = "R",
  rank_selection = c("cv", "none"),
  rank_rule = c("both", "1se", "min"),
  folds = NULL,
  select_folds = 4L,
  include_oracle = TRUE,
  detail_ranks = FALSE,
  n_test = NULL,
  penalty_order = 2L
)
}
\arguments{
\item{d}{Dimension.}

\item{n_grid}{Training grid size per margin (scalar or length-\code{d}).}

\item{k}{Basis size per margin.}

\item{ranks}{Candidate uniform TT ranks.}

\item{lambda}{Fixed smoothing (preferred) or \code{"cGCV"} for TT
(GLAM then uses fixed \(\lambda=1\) with a note — no GLAM-cGCV yet).}

\item{sigma}{Gaussian noise sd on the training grid.}

\item{max_sweeps}{ALS sweeps.}

\item{max_glam_npar}{Skip GLAM when \code{k^d} exceeds this.}

\item{degree}{B-spline degree.}

\item{seed}{RNG seed (data, test draw, CV folds).}

\item{backend}{TT backend.}

\item{rank_selection}{\code{"cv"} (default) or \code{"none"} (rank grid only; no
minCV/1SE rows).}

\item{rank_rule}{\code{"both"} (emit minCV and 1SE), \code{"1se"}, or \code{"min"}.}

\item{folds, select_folds}{Number of CV folds (\code{folds} preferred;
\code{select_folds} kept for backward compatibility).}

\item{include_oracle}{Include simulation-only \code{TT-oracle} row.}

\item{detail_ranks}{Append per-rank \verb{TT-r*} rows (for RMSE-vs-rank plots).}

\item{n_test}{Independent test sample size (default \code{min(2000, 4 * n_train)}).}

\item{penalty_order}{Difference penalty order.}
}
\value{
A \code{data.frame} of class \code{"glam_tt_compare"}. Attributes:
\code{selection}, \code{oracle_probe_time}, \code{test} (list with \code{X}, \code{truth}).
}
\description{
Fits all methods on the \strong{same training grid}, selects TT rank with
\code{\link[=tt_rank_select]{tt_rank_select()}} using \strong{training data only}, then evaluates
\(\mathrm{RMSE}\) vs \(f_{\mathrm{true}}\) on an \strong{independent test sample}.
}
\details{
\subsection{Timing (honest computational accounting)}{\tabular{ll}{
   Column \tab Meaning \cr
   \code{fit_time} \tab Cost of the \strong{final} model fit (known rank / GLAM). \cr
   \code{rank_selection_time} \tab Cost of \code{\link[=tt_rank_select]{tt_rank_select()}} CV (0 for GLAM / oracle). \cr
   \code{total_procedure_time} \tab Practical cost = \code{fit_time + rank_selection_time}. \cr
   \code{oracle_probe_time} \tab Extra cost of probing all ranks for \code{TT-oracle} (\strong{not} \cr
   included in practical totals). \tab  \cr
}


A referee can therefore see both “TT is fast once \(r\) is known” and
“selecting \(r\) by \(K\)-fold CV costs \(K\times|\mathcal R|\) fits.”
}
}
\examples{
cmp <- compare_glam_tt_gaussian(
  d = 3, n_grid = c(8, 7, 6), k = 5, ranks = 1:3,
  lambda = 1, folds = 3, max_sweeps = 4, seed = 1
)
cmp[, c("method", "rank", "fit_time", "rank_selection_time",
        "total_procedure_time", "rmse_truth")]
}
\seealso{
\code{\link[=summarize_glam_tt_compare]{summarize_glam_tt_compare()}}, \code{\link[=plot.glam_tt_compare]{plot.glam_tt_compare()}},
\code{\link[=tt_rank_select]{tt_rank_select()}}.
}
