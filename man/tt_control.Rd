% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{tt_control}
\alias{tt_control}
\title{Control parameters for \code{\link[=ttps]{ttps()}}.}
\usage{
tt_control(
  max_sweeps = 50,
  pirls_maxit = 50,
  tol = 1e-08,
  tol_lambda = 1e-04,
  lambda_start = 1,
  lambda_bounds = c(1e-04, 10000),
  lambda_tol = NULL,
  lambda_update = "auto",
  backend = c("auto", "R", "Rcpp", "keras"),
  sparse = c("auto", TRUE, FALSE),
  use_spectral_gcv = TRUE,
  outer_maxit = 20,
  outer_tol = 1e-05,
  lbfgs_maxit = 500,
  gd_lr = 0.01,
  gd_maxit = 5000L,
  gd_tol = 1e-07,
  gd_linesearch = TRUE,
  gd_step_factor = 0.5,
  gd_step_min = 1e-12,
  gd_armijo_c = 1e-04,
  adam_lr = 0.001,
  adam_epochs = 1000,
  adam_batch_size = NULL,
  adam_patience = 30,
  trace = FALSE,
  monitor = NULL,
  damping = TRUE,
  pirls_step_halving = NULL,
  step_factor = 0.5,
  step_min = 1/128,
  objective_tol = 1e-10,
  binomial_mu_eps = 1e-05,
  binomial_weight_floor = 1e-04,
  seed = 1,
  init_sd = 0.15,
  als_sweeps_per_pirls = 1,
  als_sweeps_adaptive = TRUE,
  als_sweeps_per_pirls_max = 3L,
  hybrid_lbfgs_maxit = 50L,
  dn_max_sweeps = 40L,
  dn_armijo_c = 1e-04,
  dn_step_factor = 0.5,
  dn_step_min = 1e-12,
  dn_ridge = 0,
  block_lbfgs_maxit = 50L,
  block_lbfgs_sweeps = 40L,
  compute_edf = TRUE,
  edf_max_npar = 2500L,
  warn_lambda_boundary = TRUE,
  cgcv_update = c("outer_simultaneous", "sequential"),
  cgcv_damping = 0.25,
  cgcv_max_log10_step = 1,
  cgcv_parameterization = c("free", "scale_anisotropy"),
  cgcv_trace = TRUE,
  cgcv_margin_order = NULL,
  cgcv_fit_sweeps = NULL,
  cgcv_lambda0_method = c("fixed_start", "log_grid"),
  cgcv_lambda0_grid = NULL
)
}
\arguments{
\item{max_sweeps}{Maximum ALS sweeps (Gaussian / inner GLM).}

\item{pirls_maxit}{Maximum outer PIRLS iterations (GLM).}

\item{tol}{Relative deviance / RSS change for stopping.}

\item{tol_lambda}{Relative log-λ change for cGCV outer convergence.}

\item{lambda_start}{Initial λ used when \code{lambda = "cGCV"}.}

\item{lambda_bounds}{Length-2 bounds for automatic λ search on log scale.}

\item{lambda_tol}{Alias of \code{tol_lambda}.}

\item{lambda_update}{Reserved (\code{"auto"}); future releases may extend.}

\item{backend}{\code{"auto"}, \code{"R"}, \code{"Rcpp"}, or \code{"keras"} (Adam only).}

\item{sparse}{\code{"auto"}, \code{TRUE}, or \code{FALSE} (v0: dense bases; hybrid reserved).}

\item{use_spectral_gcv}{Use spectral cache inside Brent cGCV when feasible.}

\item{outer_maxit}{Outer alternation iters (LBFGS/Adam + cGCV).}

\item{outer_tol}{Outer relative change tolerance.}

\item{lbfgs_maxit}{Max L-BFGS iterations.}

\item{gd_lr}{Initial / nominal GD step size (start of Armijo search when
\code{gd_linesearch = TRUE}).}

\item{gd_maxit}{Maximum gradient-descent iterations.}

\item{gd_tol}{Infinity-norm gradient tolerance for GD stopping.}

\item{gd_linesearch}{Use Armijo backtracking (recommended; default TRUE).}

\item{gd_step_factor}{Backtracking factor for GD line search (default 0.5).}

\item{gd_step_min}{Smallest GD step before declaring line-search failure.}

\item{gd_armijo_c}{Armijo sufficient-decrease constant (default 1e-4).}

\item{adam_lr, adam_epochs, adam_batch_size, adam_patience}{Adam/Keras knobs
(optional backend; \code{adam_batch_size = NULL} means full-batch).}

\item{trace}{Print iteration progress (\code{TRUE}/\code{FALSE}). Alias of \code{monitor}.}

\item{monitor}{Alias of \code{trace} — set \code{monitor = TRUE} to watch ALS / PIRLS /
L-BFGS / GD sweeps. If both are supplied, either \code{TRUE} enables logging.}

\item{damping}{Legacy alias: for Bernoulli, enables \code{pirls_step_halving}
when \code{pirls_step_halving} is left at default.}

\item{pirls_step_halving}{Bernoulli outer line search on the true penalized
Bernoulli objective (parameter-space blend of TT cores).}

\item{step_factor}{Multiplicative factor for step-halving (default 0.5).}

\item{step_min}{Smallest accepted step size (default 1/128).}

\item{objective_tol}{Absolute tolerance for accepting a non-increasing step.}

\item{binomial_mu_eps}{Clip Bernoulli mean away from 0/1 in PIRLS.}

\item{binomial_weight_floor}{Shared floor for Bernoulli working variance
used in both \code{W} and \code{z}.}

\item{seed}{RNG seed for TT core initialization.}

\item{init_sd}{SD for random TT initialization.}

\item{als_sweeps_per_pirls}{Inner ALS sweeps per PIRLS iteration (default
\code{1}). Extra inner sweeps multiply \code{P_k^{full}} builds; on Chicago Poisson
\(r=3\), \code{1} matched deviance of \code{3} at ~5–6× lower wall time.}

\item{als_sweeps_adaptive}{If \code{TRUE} (default), allow additional inner ALS
sweeps up to \code{als_sweeps_per_pirls_max} when the working predictor is still
moving after the base sweep count.}

\item{als_sweeps_per_pirls_max}{Cap on adaptive inner sweeps (default \code{3}).}

\item{hybrid_lbfgs_maxit}{Max L-BFGS iterations after ALS warm-start when
\code{optimizer = "hybrid"} (experimental Bernoulli polish).}

\item{dn_max_sweeps}{Max outer sweeps for Damped-Newton-ALS.}

\item{dn_armijo_c}{Armijo constant for Damped-Newton-ALS.}

\item{dn_step_factor}{Backtracking factor for Damped-Newton-ALS.}

\item{dn_step_min}{Smallest Newton step before rejecting the update.}

\item{dn_ridge}{Optional explicit ridge added to the conditional Hessian
(separate from the P-spline penalty; default 0).}

\item{block_lbfgs_maxit}{Max L-BFGS iterations per core for LBFGS-ALS.}

\item{block_lbfgs_sweeps}{Max outer ALS sweeps for LBFGS-ALS.}

\item{compute_edf}{Compute joint linearized EDF after fit (\code{TRUE}/\code{FALSE}).
Skipped automatically when packed TT size exceeds \code{edf_max_npar}.}

\item{edf_max_npar}{Maximum packed TT parameters for joint EDF (memory guard).}

\item{warn_lambda_boundary}{Soft warning when cGCV λ hits search bounds.}

\item{cgcv_update}{cGCV dynamics: \code{"outer_simultaneous"} (default; fit all
cores at fixed λ, freeze, Jacobi proposals, damped/trust update) or
\code{"sequential"} (legacy Gauss–Seidel; can cascade to λ_max on Chicago
Poisson under the global penalty).}

\item{cgcv_damping}{Log-scale mixing weight \(\rho\in(0,1]\) toward the
raw proposal (default \code{0.25}).}

\item{cgcv_max_log10_step}{Trust region: max \(|\Delta\log_{10}\lambda|\)
per update (default \code{1}). Use \code{Inf} to disable.}

\item{cgcv_parameterization}{\code{"free"} (default) or \code{"scale_anisotropy"}
(\(\lambda_m=\lambda_0\omega_m\), \(\prod\omega_m=1\)).}

\item{cgcv_trace}{Store per-update cGCV diagnostics on the fit (\code{TRUE}).}

\item{cgcv_margin_order}{Optional permutation of \code{1:d} for sequential
updates (order-sensitivity diagnostics).}

\item{cgcv_fit_sweeps}{ALS sweeps in each outer fit step (\code{NULL} →
\code{max_sweeps}).}

\item{cgcv_lambda0_method}{For scale–anisotropy: \code{"fixed_start"} or
\code{"log_grid"} overall-scale search.}

\item{cgcv_lambda0_grid}{Optional numeric grid for \code{"log_grid"}.}
}
\value{
A list of class \code{"tt_control"}.
}
\description{
Separates algorithmic knobs from the model API. Optimizer-specific fields
for L-BFGS / Adam are reserved; Adam/Keras is optional and not required
for the default ALS + cGCV workflows.
}
