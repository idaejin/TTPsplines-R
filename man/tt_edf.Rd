% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edf.R
\name{tt_edf}
\alias{tt_edf}
\title{Extract joint and per-margin EDF from a TT fit}
\usage{
tt_edf(object, recompute = FALSE, max_npar = NULL)
}
\arguments{
\item{object}{A \code{"ttpspline"} / \code{"ttps"} fit from \code{\link[=ttps]{ttps()}}.}

\item{recompute}{If \code{TRUE}, always recompute from cores (default \code{FALSE}
uses stored slots when present).}

\item{max_npar}{Memory guard for joint EDF (default from
\code{object$control$edf_max_npar} or \code{2500}).}
}
\value{
A list of class \code{"tt_edf"} with \code{joint}, \code{margin}, \code{margin_cond},
\code{sum_margin}, \code{note}, and \code{additive} (\code{TRUE} for block margins).
}
\description{
Returns the joint linearized EDF (\code{fit$edf}) and two per-margin summaries:
}
\details{
\describe{
\item{\code{margin} / \code{fit$edf_margin}}{Diagonal blocks
\(\operatorname{tr}(H_{kk})\) of the joint influence
\(H=(J^\top J+P_\lambda)^{-1}J^\top J\). These \strong{sum} to the joint EDF
(parameter-block partition of TT core coordinates — not univariate
GAM-style margin EDFs).}
\item{\code{margin_cond} / \code{fit$edf_margin_cond}}{Conditional ALS core EDFs
used inside cGCV. Diagnostic only; \(\sum_k\mathrm{ed}_k\) need not
equal \code{edf}.}
}

If \code{compute_edf} was \code{FALSE} at fit time, recomputes when \code{cores},
\code{penalties}, and bases are available (rebuilds bases from \code{fit$X}).
}
\examples{
data(ishigami)
X <- as.matrix(ishigami[1:150, c("x1", "x2", "x3")])
fit <- ttps(
  ishigami$y[1:150], X, rank = 2, k = 6, lambda = 1,
  control = tt_control(max_sweeps = 5, compute_edf = TRUE)
)
tt_edf(fit)
# sum(fit$edf_margin) ~= fit$edf
# fit$edf_margin_cond  # cGCV-style conditional traces

}
