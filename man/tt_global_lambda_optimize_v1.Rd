% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/global_lambda_optimize_v1.R
\name{tt_global_lambda_optimize_v1}
\alias{tt_global_lambda_optimize_v1}
\title{Adaptive experimental joint λ optimizer (v1).}
\usage{
tt_global_lambda_optimize_v1(
  y = NULL,
  X = NULL,
  formula = NULL,
  data = NULL,
  rank,
  family = stats::gaussian(),
  theta_lower = -5,
  theta_upper = 5,
  n_refine = 5L,
  n_diverse = 10L,
  M_search = 15L,
  M_final = 40L,
  core_starts_final = 3L,
  min_dist = 0.75,
  boundary_tol = 0.05,
  seed = 1L,
  k = 8L,
  degree = 3L,
  penalty_order = 2L,
  control = tt_control(max_sweeps = 40L, tol = 1e-08, compute_edf = FALSE, seed = 1L),
  include_cgcv_anchor = TRUE,
  extra_theta = NULL,
  epsilon_rel = 0.001,
  scheme = c("forward", "central"),
  adaptive_box = TRUE,
  max_expansions = 2L,
  expand_boundary_tol = 0.5,
  near_optimal_tol = 0.01,
  sobol_batches = c(64L, 64L, 128L),
  profile_lambda = TRUE,
  classify_identifiability = TRUE,
  expand_step = 3,
  stop_on_stable_region = TRUE,
  min_batches_before_stop = 2L,
  verbose = FALSE
)
}
\arguments{
\item{y, X}{Gaussian response and covariates (preferred API).}

\item{formula, data}{Optional formula interface (\code{y ~ x1 + x2}); ignored if
\code{y}/\code{X} supplied.}

\item{rank}{Fixed TT rank.}

\item{family}{Currently only \code{\link[stats:gaussian]{stats::gaussian()}}.}

\item{theta_lower, theta_upper}{Box bounds (scalar or length-d).}

\item{n_refine}{Number of local refinements (\code{nlminb} bounded).}

\item{n_diverse}{How many diverse Sobol elites to consider before refine.}

\item{M_search, M_final}{SAA probe counts for search vs final re-eval.}

\item{core_starts_final}{Core inits for rigorous re-eval (basin by ALS
objective, then GCV).}

\item{min_dist}{Minimum θ-distance for diversity clustering.}

\item{boundary_tol}{Distance to box edge that flags \code{boundary = TRUE}.}

\item{seed}{Master seed (common cores + MC banks).}

\item{k, degree, penalty_order}{Basis / penalty.}

\item{control}{\code{\link[=tt_control]{tt_control()}} for ALS.}

\item{include_cgcv_anchor}{If \code{TRUE}, evaluate and seed with cGCV.}

\item{extra_theta}{Optional matrix of additional θ starts (\verb{n × d}), e.g. a
prior solution used as an explicit anchor (\verb{prior_solution_*}).}

\item{epsilon_rel, scheme}{Forwarded to MC-GDF.}

\item{adaptive_box}{Expand only margins that hit the boundary.}

\item{max_expansions}{Maximum directed expansions.}

\item{expand_boundary_tol}{δ for edge detection (default 0.5).}

\item{near_optimal_tol}{α for R_α (default 0.01).}

\item{sobol_batches}{Integer vector of incremental Sobol sizes.}

\item{profile_lambda}{If TRUE, build 1-D profiles for ID labels.}

\item{classify_identifiability}{If TRUE, label each λ.}

\item{expand_step}{How much to widen a bound when expanding.}

\item{stop_on_stable_region}{If TRUE, stop Sobol batches early when the
near-optimal region intervals stabilize (default TRUE). Prefer FALSE
for higher-d validation gates.}

\item{min_batches_before_stop}{Minimum batches before early stop can fire.}

\item{verbose}{Print stage progress.}
}
\value{
Diagnostic list including region and identifiability.
}
\description{
Extends \code{\link[=tt_global_lambda_optimize]{tt_global_lambda_optimize()}} with batched Sobol, directed box
expansion, near-optimal regions, and λ identifiability labels.
Not exported. Productive path remains \code{lambda = "cGCV"}.
}
\keyword{internal}
