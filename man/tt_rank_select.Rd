% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_select.R
\name{tt_rank_select}
\alias{tt_rank_select}
\title{Data-driven TT rank selection via K-fold CV (+ optional 1-SE rule).}
\usage{
tt_rank_select(
  y,
  X,
  ranks = 1:5,
  family = stats::gaussian(),
  k = 10,
  degree = 3,
  penalty_order = 2,
  lambda = "cGCV",
  optimizer = "auto",
  backend = "auto",
  control = tt_control(),
  folds = 5L,
  rule = c("1se", "min"),
  metric = c("auto", "rmse", "poisson_deviance", "logloss"),
  seed = NULL,
  keep_fits = FALSE,
  knots = NULL,
  offset = NULL,
  rank_chain = NULL,
  ...
)
}
\arguments{
\item{y, X}{Response and covariate matrix (as in \code{\link[=ttpspline]{ttpspline()}}).}

\item{ranks}{Integer vector of candidate \strong{uniform} ranks (default \code{1:5}).}

\item{family, k, degree, penalty_order, lambda, optimizer, backend, control, knots, offset}{Passed through to \code{\link[=ttpspline]{ttpspline()}} on each fold (and on final refit).}

\item{folds}{Number of CV folds (default 5).}

\item{rule}{\code{"1se"} (default, parsimonious) or \code{"min"} (minimum mean CV).}

\item{metric}{\code{"auto"} (family-aware) or one of \code{"rmse"}, \code{"poisson_deviance"},
\code{"logloss"}.}

\item{seed}{Optional RNG seed for fold assignment (reproducible splits).}

\item{keep_fits}{If \code{TRUE}, store fold fits (large); default \code{FALSE}.}

\item{rank_chain}{Reserved for future non-uniform rank search; must be \code{NULL}
in this version.}

\item{...}{Currently unused (reserved).}
}
\value{
An object of class \code{"tt_rank_selection"}.
}
\description{
Chooses a uniform TT rank \(r\) (chain \verb{(1,r,...,r,1)}) using out-of-sample
predictive loss only — never simulation truth. Fitting stays separate:
\code{\link[=ttpspline]{ttpspline()}} still requires an explicit \code{rank}; use \code{\link[=tt_rank_refit]{tt_rank_refit()}} after
selection to fit on all data at the chosen rank.
}
\details{
Inner smoothing (\code{lambda} fixed or \code{"cGCV"}) is estimated \strong{only} on each
fold's training portion (no validation leakage into cGCV).
}
\section{Complexity layers}{

Rank \(r\) = structural capacity; \(\lambda\) = directional smoothness;
EDF = effective fitted flexibility. These are not interchangeable:
\(r \neq \lambda \neq \mathrm{EDF}\).
}

\examples{
data(friedman)
X <- as.matrix(friedman[1:200, paste0("x", 1:5)])
y <- friedman$y[1:200]
sel <- tt_rank_select(
  y, X, ranks = 1:3, k = 5, lambda = 1, folds = 3, rule = "1se",
  seed = 1, control = tt_control(max_sweeps = 4, compute_edf = FALSE)
)
sel
fit <- tt_rank_refit(sel)

}
\seealso{
\code{\link[=tt_rank_refit]{tt_rank_refit()}}, \code{\link[=ttpspline]{ttpspline()}}, \code{\link[=tt_rank_profile]{tt_rank_profile()}} (in-sample
rank diagnostic, not CV).
}
