% Generated by hand (NAMESPACE is hand-managed in v0).
\name{ttps_multistart}
\alias{ttps_multistart}
\title{Multi-start TT-P-spline fits (nonconvex diagnostics)}
\usage{
ttps_multistart(
  y,
  X,
  family = stats::gaussian(),
  rank = 3,
  k = 10,
  degree = 3,
  penalty_order = 2,
  lambda = "cGCV",
  optimizer = "auto",
  backend = "auto",
  control = tt_control(),
  monitor = FALSE,
  knots = NULL,
  offset = NULL,
  weights = NULL,
  cyclic = NULL,
  period = NULL,
  n_starts = 10L,
  seed = 1L,
  select = c("objective", "deviance"),
  prefer_converged = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{y, X, family, rank, k, degree, penalty_order, lambda, optimizer, backend, control, monitor, knots, offset, weights, cyclic, period}{Passed to \code{\link{ttps}}.}

\item{n_starts}{Number of random TT initializations (default \code{10}).}

\item{seed}{Base RNG seed; start \code{s} uses \code{seed + s - 1}.}

\item{select}{Criterion for the retained fit: \code{"objective"} (default) or \code{"deviance"}.}

\item{prefer_converged}{Prefer converged starts when comparing criteria.}

\item{verbose}{Print progress per start.}
}
\value{
An object of class \code{"ttps_multistart"} with \code{best}, \code{starts},
\code{frac_boundary}, and settings.
}
\description{
Runs \code{\link{ttps}} from several random TT initializations and returns the
best fit with a start-wise summary. Useful for \eqn{\lambda}-bound and
low-rank ALS diagnostics. Does not change the default single-start
\code{ttps()} path.
}
\seealso{
\code{\link{ttps}}, \code{\link{tt_rank_select}}, \code{\link{tt_initialize}}
}
