% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_profile.R
\name{tt_rank_profile}
\alias{tt_rank_profile}
\title{Fit TT P-splines over a grid of ranks (in-sample rank diagnostic).}
\usage{
tt_rank_profile(
  y,
  X,
  ranks = c(1, 2, 3, 4, 6),
  family = stats::gaussian(),
  lambda = "cGCV",
  k = 10,
  control = tt_control(),
  ...
)
}
\arguments{
\item{y}{Numeric response (\code{0/1} for binomial; counts for Poisson).}

\item{X}{Numeric matrix / data frame of continuous covariates (\verb{n × d}).}

\item{ranks}{Numeric vector of scalar ranks to try.}

\item{family}{A \code{\link[stats:family]{stats::family()}} object or one of
\code{"gaussian"}, \code{"poisson"}, \code{"binomial"}.}

\item{lambda}{Numeric (isotropic / anisotropic fixed) or \code{"cGCV"}.
\code{"cFS"} / \code{"cREML"} are not implemented yet.}

\item{k}{Number of B-spline basis functions per margin.}

\item{control}{A \code{\link[=tt_control]{tt_control()}} list.}

\item{...}{Passed to \code{\link[=ttpspline]{ttpspline()}} (overrides \code{rank}).}
}
\value{
Object of class \code{"tt_rank_profile"}.
}
\description{
Sweeps ranks on the \strong{full} training sample and reports deviance /
complexity. This is \strong{not} cross-validated predictive selection; for
that use \code{\link[=tt_rank_select]{tt_rank_select()}}.
}
\seealso{
\code{\link[=tt_rank_select]{tt_rank_select()}}
}
