% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ttpspline.R
\name{ttps}
\alias{ttps}
\alias{ttpspline}
\title{Tensor-Train P-spline for multidimensional smooth / GLM regression.}
\usage{
ttps(
  y,
  X,
  family = stats::gaussian(),
  rank = 3,
  k = 10,
  degree = 3,
  penalty_order = 2,
  lambda = "cGCV",
  optimizer = c("auto", "ALS", "PIRLS-ALS", "Damped-Newton-ALS", "LBFGS-ALS", "GD",
    "LBFGS", "hybrid", "Adam"),
  backend = c("auto", "R", "Rcpp", "keras"),
  init = NULL,
  control = tt_control(),
  monitor = FALSE,
  knots = NULL,
  offset = NULL,
  linear = NULL,
  weights = NULL,
  cyclic = NULL,
  period = NULL
)

ttpspline(
  y,
  X,
  family = stats::gaussian(),
  rank = 3,
  k = 10,
  degree = 3,
  penalty_order = 2,
  lambda = "cGCV",
  optimizer = c("auto", "ALS", "PIRLS-ALS", "Damped-Newton-ALS", "LBFGS-ALS", "GD",
    "LBFGS", "hybrid", "Adam"),
  backend = c("auto", "R", "Rcpp", "keras"),
  init = NULL,
  control = tt_control(),
  monitor = FALSE,
  knots = NULL,
  offset = NULL,
  linear = NULL,
  weights = NULL,
  cyclic = NULL,
  period = NULL
)
}
\arguments{
\item{y}{Numeric response (\code{0/1} for binomial; counts for Poisson).}

\item{X}{Numeric matrix / data frame of continuous covariates (\verb{n × d}).}

\item{family}{A \code{\link[stats:family]{stats::family()}} object or one of
\code{"gaussian"}, \code{"poisson"}, \code{"binomial"}.}

\item{rank}{TT rank (scalar or length \code{d-1}); see \code{\link[=tt_rank]{tt_rank()}}.
\strong{Not} selected automatically — \code{rank = 2} means exactly rank 2.
For data-driven choice use \code{\link[=tt_rank_select]{tt_rank_select()}} then \code{\link[=tt_rank_refit]{tt_rank_refit()}}.}

\item{k}{Number of B-spline basis functions per margin.}

\item{degree}{B-spline degree.}

\item{penalty_order}{Difference penalty order.}

\item{lambda}{Numeric (isotropic / anisotropic fixed) or \code{"cGCV"}.
\code{"cFS"} / \code{"cREML"} are not implemented yet.}

\item{optimizer}{One of:
\itemize{
\item \code{"auto"} — documented family default:
Gaussian \(\to\) \code{"ALS"}, Poisson \(\to\) \code{"PIRLS-ALS"},
binomial \(\to\) \code{"LBFGS"} (see \code{optimizer_requested} /
\code{optimizer_used} on the fit);
\item \code{"ALS"} / \code{"PIRLS-ALS"} — structure-aware ALS / PIRLS+ALS;
\item \code{"Damped-Newton-ALS"} — conditional Newton + Armijo on true \(Q_k\);
\item \code{"LBFGS-ALS"} — block/core-wise L-BFGS on each \(Q_k\) (≠ global LBFGS);
\item \code{"GD"} — global first-order on \(\mathcal L\) (diagnostic);
\item \code{"LBFGS"} — global quasi-Newton on \(\mathcal L\);
\item \code{"hybrid"} — experimental ALS→LBFGS polish;
\item \code{"Adam"} — optional Keras (not yet implemented).
}}

\item{backend}{\code{"auto"}, \code{"R"}, \code{"Rcpp"}, or \code{"keras"}. Overridden by
\code{control$backend} only when this argument is \code{"auto"} and control is not;
prefer setting backend here or in \code{\link[=tt_control]{tt_control()}}.}

\item{init}{Optional TT cores from \code{\link[=tt_initialize]{tt_initialize()}} for fair optimizer
comparisons; \code{NULL} draws from \code{control$seed}.}

\item{control}{A \code{\link[=tt_control]{tt_control()}} list.}

\item{monitor}{If \code{TRUE}, print iteration progress (sets \code{control$trace}).
Convenient alias of \code{tt_control(monitor = TRUE)} / \code{tt_control(trace = TRUE)}.}

\item{knots}{Optional list of knot vectors (advanced).}

\item{offset}{Optional numeric vector of length \code{n} (or scalar) added to the
linear predictor, e.g. \code{log(exposure)} for Poisson. Default \code{NULL} (zeros).}

\item{linear}{Optional unpenalized parametric design matrix (\verb{n × p}), e.g.
\code{model.matrix(~ 0 + dow + ns(time, df), data)}. Do \strong{not} include an
intercept column (\code{ttps} already estimates one). Estimated jointly with the
TT smooth via block-coordinate updates (ALS / PIRLS-ALS). Currently
unsupported for LBFGS / GD / hybrid / Adam / LBFGS-ALS / DN-ALS (error).}

\item{weights}{Optional non-negative observation weights of length \code{n}
(or scalar). \code{NULL} means all ones. Zero weights exclude observations from
the likelihood (useful for masking empty array cells).}

\item{cyclic}{Logical scalar or length-\code{d} flags for \strong{circular} P-spline
margins (periodic B-spline basis + circular difference penalty). For
hour-of-day, map to \(\link{0,1}\) and set that margin to \code{TRUE}.}

\item{period}{Optional length-\code{d} list of \code{c(xl,xr)} for cyclic margins.}
}
\value{
An object of class \code{"ttpspline"}.
}
\description{
Fits a \strong{non-additive} tensor-product P-spline surface whose coefficient
array is represented in Tensor-Train (TT) format. Observations may be
arbitrarily scattered in \eqn{[a,b]^d}; no data grid is required.
}
\details{
The preferred name is \code{\link[=ttps]{ttps()}}. \code{\link[=ttpspline]{ttpspline()}} is an identical alias kept
for backward compatibility.

Three orthogonal choices:
\itemize{
\item \code{optimizer}: estimation philosophy —
structure-aware \code{ALS} / \code{PIRLS-ALS}, or direct penalized
likelihood \code{GD} / \code{LBFGS} / \code{Adam}.
\code{auto} is a simple family-aware default (Gaussian \(\to\) ALS,
Poisson \(\to\) PIRLS-ALS, binomial \(\to\) LBFGS); always overridable.
\item \code{lambda}: fixed isotropic/anisotropic or automatic \code{"cGCV"}
\item \code{backend}: computational engine (\code{auto} / \code{R} / \code{Rcpp} / \code{keras})
}
}
\examples{
## Packaged classic surfaces
data(ishigami)
X <- as.matrix(ishigami[, c("x1", "x2", "x3")])
fit <- ttps(ishigami$y, X, rank = 2, k = 6, lambda = 1,
            control = tt_control(max_sweeps = 6, compute_edf = FALSE))
summary(fit)

data(sobol_g)
data(friedman)

## On-the-fly simulation (Gaussian / Poisson / Bernoulli)
set.seed(1)
n <- 400
X <- matrix(runif(n * 3), n, 3)
f <- sin(2 * pi * X[, 1]) * cos(2 * pi * X[, 2]) + X[, 3]

## Gaussian (auto -> ALS)
y <- f + rnorm(n, 0, 0.3)
fit_g <- ttps(y, X, family = gaussian(), rank = 2, k = 6,
              lambda = 1, control = tt_control(max_sweeps = 8))
tt_complexity(fit_g)

## Poisson (auto -> PIRLS-ALS)
yp <- rpois(n, exp(f - mean(f) + log(2)))
fit_p <- ttps(yp, X, family = poisson(), rank = 2, k = 6, lambda = 1,
              control = tt_control(pirls_maxit = 15, compute_edf = FALSE))
fit_p$optimizer_used

## Bernoulli (auto -> LBFGS)
yb <- rbinom(n, 1, plogis(1.2 * (f - mean(f))))
fit_b <- ttps(yb, X, family = binomial(), rank = 2, k = 6, lambda = 5,
              control = tt_control(lbfgs_maxit = 100, compute_edf = FALSE))
fit_b$optimizer_used
predict(fit_b, X[1:3, ], type = "response")

## Watch iteration progress
\dontrun{
fit_m <- ttps(ishigami$y, X, rank = 2, k = 6, lambda = 1,
              monitor = TRUE, control = tt_control(max_sweeps = 8))
}

}
