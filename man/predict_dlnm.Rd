% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dlnm.R
\name{predict_dlnm}
\alias{predict_dlnm}
\title{Predict overall / lag-slice cumulative effects from a \code{\link[=ttps_dlnm]{ttps_dlnm()}} fit.}
\usage{
predict_dlnm(
  fit,
  var,
  at,
  cen = NULL,
  type = c("overall", "slice"),
  lag_at = 0L,
  se = FALSE
)
}
\arguments{
\item{fit}{A \code{"ttps_dlnm"} object.}

\item{var}{Name of exposure to vary (must be in \code{fit$dlnm$names}).}

\item{at}{Grid of values for \code{var}.}

\item{cen}{Named vector / list of centering values for all exposures
(defaults to training medians of unlagged series). \code{cen[[var]]} is the
reference for RR.}

\item{type}{\code{"overall"} (sum over lags; Gasparrini overall) or \code{"slice"}
(single lag).}

\item{lag_at}{Lag value for \code{type = "slice"} (in \code{0:L}).}

\item{se}{Currently ignored (Level-1 SE for DLNM not yet wired).}
}
\value{
A data.frame with \code{x}, \code{logRR}, \code{RR} (and \code{lag} for slices).
}
\description{
Predict overall / lag-slice cumulative effects from a \code{\link[=ttps_dlnm]{ttps_dlnm()}} fit.
}
