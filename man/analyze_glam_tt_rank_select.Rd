% Generated by hand (keep in sync with R/glam.R)
\name{analyze_glam_tt_rank_select}
\alias{analyze_glam_tt_rank_select}
\title{Analyze GLAM-vs-TT with data-driven rank selection (Paper 1 step 1)}
\usage{
analyze_glam_tt_rank_select(tab)
}
\arguments{
\item{tab}{A \code{data.frame} from \code{\link{compare_glam_tt_gaussian}} or
\code{\link{compare_glam_tt_scale}} with CV-selected TT rows.}
}
\value{
An object of class \code{"glam_tt_rank_select_analysis"} with elements
\code{table}, \code{aggregate}, and \code{messages}.
}
\description{
Takes output from \code{\link{compare_glam_tt_gaussian}} /
\code{\link{compare_glam_tt_scale}} (which already fit \code{TT-minCV} and
\code{TT-1SE} via \code{\link{tt_rank_select}}, with optional \code{TT-oracle}
as simulation reference) and returns the central table
Oracle | CV-min | 1-SE | GLAM plus parsimony / agreement diagnostics.
}
\examples{
tab <- compare_glam_tt_gaussian(
  d = 3, n_grid = c(8, 7, 6), k = 5, ranks = 1:2,
  max_sweeps = 3, select_folds = 3
)
analyze_glam_tt_rank_select(tab)
}
\seealso{
\code{\link{summarize_glam_tt_compare}}, \code{\link{tt_rank_select}}
}
