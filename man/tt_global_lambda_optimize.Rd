% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/global_lambda_optimize.R
\name{tt_global_lambda_optimize}
\alias{tt_global_lambda_optimize}
\title{Experimental joint global λ optimizer (Sobol + refine + SAA TT-gGCV).}
\usage{
tt_global_lambda_optimize(
  y = NULL,
  X = NULL,
  formula = NULL,
  data = NULL,
  rank,
  family = stats::gaussian(),
  theta_lower = -6,
  theta_upper = 6,
  n_global = NULL,
  n_refine = 5L,
  n_diverse = 10L,
  M_search = 25L,
  M_final = 200L,
  core_starts_search = 1L,
  core_starts_final = 5L,
  min_dist = 0.75,
  boundary_tol = 0.05,
  seed = 1L,
  k = 8L,
  degree = 3L,
  penalty_order = 2L,
  control = tt_control(max_sweeps = 40L, tol = 1e-08, compute_edf = FALSE, seed = 1L),
  include_cgcv_anchor = TRUE,
  extra_theta = NULL,
  epsilon_rel = 0.001,
  scheme = c("forward", "central"),
  verbose = FALSE
)
}
\arguments{
\item{y, X}{Gaussian response and covariates (preferred API).}

\item{formula, data}{Optional formula interface (\code{y ~ x1 + x2}); ignored if
\code{y}/\code{X} supplied.}

\item{rank}{Fixed TT rank.}

\item{family}{Currently only \code{\link[stats:gaussian]{stats::gaussian()}}.}

\item{theta_lower, theta_upper}{Box bounds (scalar or length-d).}

\item{n_global}{Sobol sample size (default by \code{d} if \code{NULL}).}

\item{n_refine}{Number of local refinements (\code{nlminb} bounded).}

\item{n_diverse}{How many diverse Sobol elites to consider before refine.}

\item{M_search, M_final}{SAA probe counts for search vs final re-eval.}

\item{core_starts_search}{Core inits per Sobol/refine eval (default 1).}

\item{core_starts_final}{Core inits for rigorous re-eval (basin by ALS
objective, then GCV).}

\item{min_dist}{Minimum θ-distance for diversity clustering.}

\item{boundary_tol}{Distance to box edge that flags \code{boundary = TRUE}.}

\item{seed}{Master seed (common cores + MC banks).}

\item{k, degree, penalty_order}{Basis / penalty.}

\item{control}{\code{\link[=tt_control]{tt_control()}} for ALS.}

\item{include_cgcv_anchor}{If \code{TRUE}, evaluate and seed with cGCV.}

\item{extra_theta}{Optional matrix of additional θ starts (\verb{n × d}), e.g. a
prior solution used as an explicit anchor (\verb{prior_solution_*}).}

\item{epsilon_rel, scheme}{Forwarded to MC-GDF.}

\item{verbose}{Print stage progress.}
}
\value{
Unevaluated diagnostic list (see package lab docs).
}
\description{
Optimizes \(\boldsymbol\theta=\log_{10}\boldsymbol\lambda\) jointly inside a
compact box. Not exported. Does not replace operational \code{cGCV}.
}
\keyword{internal}
