% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dlnm.R
\name{ttps_dlnm}
\alias{ttps_dlnm}
\title{Tensor-Train DLNM (distributed lag non-linear model)}
\usage{
ttps_dlnm(
  y,
  x,
  lag = 14L,
  family = stats::poisson(),
  rank = 1L,
  k = 8L,
  k_lag = NULL,
  degree = 3L,
  penalty_order = 2L,
  lambda = 1,
  linear = NULL,
  smooth = NULL,
  lambda_smooth = "cGCV",
  offset = NULL,
  weights = NULL,
  control = tt_control(),
  ...
)
}
\arguments{
\item{y}{Response vector (length \(n\)).}

\item{x}{Named list of exposure vectors (each length \(n\)), e.g.
\code{list(temp = d$temp, pm10 = d$pm10, o3 = d$o3)}.}

\item{lag}{Maximum lag \(L\) (non-negative integer). Histories use
\(\ell=0,\ldots,L\).}

\item{family}{A family object (\code{poisson()}, \code{gaussian()}, …).}

\item{rank}{TT rank (scalar or compatible chain).}

\item{k}{Basis size for each \strong{exposure} margin.}

\item{k_lag}{Basis size for the lag margin (defaults to \code{k}).}

\item{degree}{B-spline degree.}

\item{penalty_order}{Difference penalty order.}

\item{lambda}{Fixed nonnegative scalar / length-\code{d} vector, or \code{"cGCV"}
(conditional GCV on each TT core with summed lag designs).}

\item{linear}{Optional parametric design (\code{dow}, …); see \code{\link[=ttps]{ttps()}}.}

\item{smooth}{Optional additive smooths (e.g. year + month); see \code{\link[=ttps]{ttps()}}.}

\item{lambda_smooth}{Default for smooth terms.}

\item{offset, weights}{Optional offset / observation weights.}

\item{control}{A \code{\link[=tt_control]{tt_control()}} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{c("ttps_dlnm", "ttpspline")} with extra
components \code{dlnm} (lag histories, knots, names) for prediction.
}
\description{
Fits
\deqn{\eta_t = \mathrm{offset}_t + \alpha + Z_t\beta + s(t)
  + \sum_{\ell=0}^{L} h(x^{(1)}_{t-\ell},\ldots,x^{(p)}_{t-\ell},\ell)}
where \(h\) is a tensor-product P-spline with coefficient tensor \(\Theta\)
in TT format. The dense cross-basis \(W\) is \strong{never} formed: each ALS /
PIRLS step contracts the TT against lag-specific marginal bases and sums
over \(\ell\).
}
\details{
Calendar confounding belongs in \verb{smooth=} / \verb{linear=} (e.g.
\code{s(year)+s(month,cc)+dow}), not inside the lag surface.
}
\examples{
\dontrun{
data(chicagoNMMAPS, package = "dlnm")
d <- na.omit(chicagoNMMAPS[, c("death","temp","pm10","o3","year","month","dow")])
fit <- ttps_dlnm(
  d$death,
  list(temp = d$temp, pm10 = d$pm10, o3 = d$o3),
  lag = 14, family = poisson(), rank = 1, k = 6, k_lag = 5,
  lambda = 1,
  linear = model.matrix(~ 0 + dow, d),
  smooth = list(
    year = list(x = d$year, bs = "ps", k = 10, m = 2, target_edf = 6),
    month = list(x = d$month, bs = "cc", k = 12, m = 2,
                 period = c(0.5, 12.5), target_edf = 6)
  ),
  control = tt_control(max_sweeps = 8, pirls_maxit = 10, compute_edf = FALSE)
)
pr <- predict_dlnm(fit, var = "temp", at = seq(-10, 30, by = 2), cen = 21)
}
}
